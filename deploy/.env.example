# Copy to .env beside docker-compose.yml on the server.

# The VM's DNS name, e.g. walkreach.australiaeast.cloudapp.azure.com
DOMAIN=

# Goes into a connection URL, so letters and digits only:
#   openssl rand -hex 24
POSTGRES_PASSWORD=
